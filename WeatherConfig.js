// The temperature when a kat considers it cold
const coldCelcius = 20;

// The temp when a kat will walk down to doubledouble without a second though.
const noiceCelcius = 25;

// The temperature when a kat considers it hot
const hotCelcius = 30;

// The temperature when a kat should just stick to the warrn
const reallyHotCelcius = 40;

// The thumidity when a kat thinks it feels 'yucky' outside
const tooHumidPercentage = 95;

module.exports = {
  coldCelcius,
  noiceCelcius,
  hotCelcius,
  reallyHotCelcius,
  tooHumidPercentage
};
